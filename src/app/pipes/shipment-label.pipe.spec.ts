import { ShipmentLabelPipe } from './shipment-label.pipe';

describe('ShipmentLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
